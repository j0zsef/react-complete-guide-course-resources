const EventsPage = () => {
    //useEffect to get the events from the backend
    //display the events with EventsList component

    return (
        <div>
        <h1>Events</h1>

        </div>
    );
}

export default EventsPage;
